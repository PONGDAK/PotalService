<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
    <select id="listAll" resultType="com.portal.service.model.board.dto.BoardDTO">
select b.id,userid,title,name,post_date,view_count from board b, member m
where b.id_member=m.id order by b.id desc
	</select>

    <insert id="insert">
        insert into board (id_member, title, content) values (#{id_member}, #{title}, #{content})
    </insert>
</mapper>














